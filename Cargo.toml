[package]
name = "radicle_registry"
version = "0.1.0"
authors = ["Monadic GmbH <radicle@monadic.xyz>"]
edition = "2018"
license = "GPL-3.0-only"

[dependencies]

[workspace]
members = [
  "cli",
  "client",
  "node",
  "runtime",
  "subxt",
  "registry-spec",
  ]
